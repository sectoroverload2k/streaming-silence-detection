# Example only - do not commit real credentials. Create with:
#   kubectl create secret generic silence-detect-station-abc123 \
#     --from-literal=stream-username=stream \
#     --from-literal=stream-password='p@ssword' \
#     --from-literal=webhook-api-key=...
apiVersion: v1
kind: Secret
metadata:
  name: silence-detect-station-abc123
type: Opaque
stringData:
  stream-username: stream
  stream-password: changeme
  webhook-api-key: changeme
