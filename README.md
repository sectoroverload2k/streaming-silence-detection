# cla-signatures

Signature records only. No code lives on this branch.

Signatures are kept off `main` so that `main` can require pull requests. The CLA
action commits directly to whichever branch holds the signature file, and a
branch that requires a pull request would reject that write.

Do not protect this branch. Do not merge it into `main`.
